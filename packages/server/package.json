{
  "name": "@aurelith/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Die beiden Serveranwendungen: Spielserver (ein Kanal, treibt die Simulation) und Anmeldeserver (Konten und Kanalliste). Zwei Prozesse, ein Paket — Konten, Datenbank und Passwortregeln stehen damit an genau einer Stelle.",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "dev:login": "tsx watch src/login/index.ts",
    "start:login": "tsx src/login/index.ts",
    "migrate": "tsx src/db/migrate.ts master",
    "migrate:welt": "tsx src/db/migrate.ts welt"
  },
  "dependencies": {
    "@aurelith/core": "*",
    "@aurelith/shared": "*",
    "pg": "^8.16.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/pg": "^8.15.0",
    "@types/ws": "^8.18.0"
  }
}
